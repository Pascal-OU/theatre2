{% extends 'base.html.twig' %}

{% block title %}Evenement{% endblock %}

{% block body %}
    <div class="container p-5 my-3">
        <h1 class="bg-secondary text-center p-3">Evenement</h1>

        <table class="table">
            <tbody class="border">
                <tr>
                    <th class="w-25 border-end text-center">Id</th>
                    <td class="w-75 text-center">{{ evenement.id }}</td>
                </tr>
                <tr>
                    <th class="w-25 border-end text-center">Title</th>
                    <td class="w-75 text-center">{{ evenement.title }}</td>
                </tr>
                <tr>
                    <th class="w-25 border-end text-center">Description</th>
                    <td class="w-75 text-center">{{ evenement.description }}</td>
                </tr>
                <tr>
                    <th class="w-25 border-end text-center">CreatedAt</th>
                    <td class="w-75 text-center">{{ evenement.createdAt ? evenement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th class="w-25 border-end text-center">NbPlace</th>
                    <td class="w-75 text-center">{{ evenement.nbPlace }}</td>
                </tr>
                <tr>
                    <th class="w-25 border-end text-center">Prix</th>
                    <td class="w-75 text-center">{{ evenement.prix }}€</td>
                </tr>
            </tbody>
        </table>
        <div class="d-flex flex-column text-center">
            <a class="" href="{{ path('app_evenement_index') }}">back to list</a>

            <a class="mt-3" href="{{ path('app_evenement_edit', {'id': evenement.id}) }}">edit</a>
        </div>
        
        <div class="text-center mt-3">
            {{ include('evenement/_delete_form.html.twig') }}
        </div>

    </div>
    
{% endblock %}
